import { generatePromoSuggestions } from '../../domain/promoEngine';import { generateManagerRecommendations } from '../../domain/copilot';
export default function TriggerEngineView({state}:{state:any}){const promos=generatePromoSuggestions(state);const recs=generateManagerRecommendations(state);return <div className='space-y-2'><p className='text-sm text-amber-400'>Manager approval required for all suggestions.</p>{promos.map(p=><div key={p.title} className='p-2 rounded bg-zinc-900'>{p.title}</div>)}{recs.map(r=><div key={r.title} className='p-2 rounded bg-zinc-900'>{r.recommendedAction}</div>)}</div>;}
